# Razel Jade Hijastro — Minimal Portfolio

This is a separate minimalist portfolio edition designed for GitHub Pages. It uses plain HTML, CSS, and JavaScript, with no build step or dependencies.

## Publish

Upload all files and the complete `assets` folder to the root of a public GitHub repository. In **Settings → Pages**, choose **Deploy from a branch**, then select `main` and `/ (root)`.
